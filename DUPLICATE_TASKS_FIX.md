# ИСПРАВЛЕНИЕ ДУБЛИРОВАНИЯ ЗАДАЧ

**Дата:** 2025-01-08  
**Проблема:** Много дублирующихся задач создано одновременно (5-6 задач "Пауза" в одну секунду)  
**Статус:** ✅ Исправлено

---

## 🔍 ПРОБЛЕМА

### Симптомы:
- Много задач "Пауза" создано в одно и то же время
- Пример: 08.01.2026, 16:37:39-40 - 5-6 задач одновременно
- Пример: 08.01.2026, 17:08-17:18 - множество задач с интервалом в 30 секунд

### Причина:
1. **Race condition в frontend:** `pauseTracking()` вызывался несколько раз до того, как `isLoading` устанавливался в `true`
2. **Нет защиты на уровне Rust:** Даже если frontend защищен, несколько вызовов могли пройти одновременно
3. **Нет проверки дубликатов в БД:** Одинаковые задачи добавлялись в очередь без проверки

---

## ✅ РЕШЕНИЕ

### 1. Защита от дублирования на уровне Rust (БД)

**Файл:** `src-tauri/src/lib.rs`

**Изменения:**
- Добавлена проверка дубликатов в `enqueue_sync()`
- Если такая же задача (entity_type + payload) уже в очереди (pending) за последние 5 секунд, она не добавляется
- Возвращается ID существующей задачи вместо создания новой

**Код:**
```rust
fn enqueue_sync(&self, entity_type: &str, payload: &str) -> SqliteResult<i64> {
    // Проверяем, есть ли такая же задача в очереди (pending) за последние 5 секунд
    let duplicate_check: i32 = conn.query_row(
        "SELECT COUNT(*) FROM sync_queue 
         WHERE entity_type = ?1 
         AND payload = ?2 
         AND status = 'pending' 
         AND created_at > ?3",
        params![entity_type, payload, now - duplicate_window],
        |row| row.get(0),
    )?;

    if duplicate_check > 0 {
        // Такая же задача уже в очереди - не добавляем дубликат
        warn!("[DB] Duplicate task detected: {} (skipping)", entity_type);
        // Возвращаем ID существующей задачи
        let existing_id: i64 = conn.query_row(...)?;
        return Ok(existing_id);
    }
    // ... добавление задачи
}
```

**Преимущества:**
- ✅ Защита на уровне БД (последний уровень защиты)
- ✅ Работает даже если frontend пропустил дубликат
- ✅ Окно дубликатов: 5 секунд (настраивается)

---

### 2. Улучшенная защита в frontend

**Файл:** `src/store/useTrackerStore.ts`

**Изменения:**
- `isLoading` устанавливается **СРАЗУ** в начале `pauseTracking()`, до любых async операций
- Дополнительная проверка состояния после установки `isLoading` (double-check)
- Это предотвращает race condition между проверкой и установкой флага

**Код:**
```typescript
pauseTracking: async (isIdlePause: boolean = false) => {
  // Проверка 1: уже загружается?
  if (currentLoading) {
    return; // Пропускаем
  }
  
  // Устанавливаем isLoading СРАЗУ (защита от race condition)
  set({ isLoading: true });
  
  // Проверка 2: еще раз проверяем состояние после блокировки
  const stateAfterLock = get();
  if (stateAfterLock.isPaused && !isIdlePause) {
    set({ isLoading: false });
    return; // Пропускаем
  }
  
  // ... остальная логика
}
```

**Преимущества:**
- ✅ Блокировка устанавливается до async операций
- ✅ Double-check после блокировки
- ✅ Предотвращает race condition

---

### 3. Улучшенное отображение в диалоге

**Файл:** `src/components/FailedTasksDialog.tsx`

**Изменения:**
- Если `error_message` отсутствует (старые задачи), показывается предупреждение
- Это помогает понять, почему нет причины ошибки

**Код:**
```typescript
{task.error_message ? (
  <div className="...">
    Причина ошибки: {task.error_message}
  </div>
) : (
  <div className="...">
    ⚠️ Причина ошибки не сохранена (старая задача)
  </div>
)}
```

---

## 📊 РЕЗУЛЬТАТ

### До исправления:
- ❌ Много дублирующихся задач (5-6 в одну секунду)
- ❌ Race condition в frontend
- ❌ Нет защиты на уровне БД

### После исправления:
- ✅ Защита на уровне БД (последний уровень)
- ✅ Улучшенная защита в frontend (ранний `isLoading`)
- ✅ Дубликаты не создаются (возвращается ID существующей задачи)
- ✅ Логирование дубликатов для диагностики

---

## 🧪 ТЕСТИРОВАНИЕ

### Как проверить:
1. **Быстрые клики:** Быстро нажмите "Пауза" несколько раз подряд
   - Ожидается: только одна задача в очереди
   - Проверка: откройте диалог failed задач и посмотрите количество

2. **Одновременные вызовы:** Откройте DevTools и выполните несколько `pauseTracking()` одновременно
   - Ожидается: только одна задача в очереди
   - Проверка: логи в терминале должны показать "Duplicate task detected"

3. **Проверка логов:** В терминале должны быть сообщения:
   - `[DB] Duplicate task detected: time_entry_pause (skipping)`
   - Это означает, что защита работает

---

## 📝 ПРИМЕЧАНИЯ

1. **Окно дубликатов:** 5 секунд - это настраиваемый параметр. Если нужно изменить, измените `duplicate_window` в `enqueue_sync()`.

2. **Старые задачи:** Задачи, созданные до этого исправления, останутся в БД. Они не будут удалены автоматически, но новые дубликаты не будут создаваться.

3. **Производительность:** Проверка дубликатов добавляет один SQL запрос. Это минимальное влияние на производительность.

---

**Дата создания:** 2025-01-08
