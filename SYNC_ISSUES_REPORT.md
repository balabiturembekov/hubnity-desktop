# ОТЧЕТ О ПРОБЛЕМАХ СИНХРОНИЗАЦИИ

**Дата:** 2025-01-08  
**Статус:** Анализ завершен

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **Ограничение на количество задач за раз (LIMIT 10)**

**Проблема:**
- `get_retry_tasks()` возвращает максимум 10 задач за раз
- Фоновая синхронизация запускается каждую минуту
- Если в очереди > 10 задач, они будут обрабатываться очень медленно

**Пример:**
- 100 pending задач → потребуется 10 минут для обработки всех
- При создании новых задач быстрее, чем обрабатываются старые, очередь будет расти

**Код:**
```rust
// src-tauri/src/lib.rs:2813
LIMIT 10
```

**Решение:**
- Увеличить LIMIT до 50-100 для фоновой синхронизации
- Или обрабатывать все задачи за один цикл (убрать LIMIT)

---

### 2. **Нет защиты от параллельных вызовов sync_queue**

**Проблема:**
- `sync_queue()` может быть вызван одновременно из нескольких мест:
  - Фоновая задача (каждую минуту)
  - `sync_queue_now()` (ручной вызов)
  - `retry_failed_tasks()` (после сброса failed задач)
- Это может привести к:
  - Дублированию обработки задач
  - Race conditions при обновлении статуса
  - Некорректному retry_count

**Код:**
```rust
// src-tauri/src/lib.rs:3204
async fn sync_queue(&self, max_retries: i32) -> Result<usize, String> {
    // Нет мьютекса или флага "синхронизация в процессе"
}
```

**Решение:**
- Добавить `Arc<Mutex<bool>>` флаг `is_syncing`
- Или использовать `Arc<tokio::sync::Mutex<()>>` для блокировки

---

### 3. **Токены могут истекать во время синхронизации**

**Проблема:**
- Токены сохраняются в payload при создании задачи
- Если задача долго лежит в очереди, токен может истечь
- `sync_task()` обновляет токен только при 401, но не проверяет срок действия заранее

**Сценарий:**
1. Задача создана с токеном (срок действия: 1 час)
2. Задача лежит в очереди 2 часа
3. При синхронизации токен уже истек → 401 ошибка
4. Refresh token тоже может истечь

**Решение:**
- Проверять срок действия токена перед синхронизацией
- Обновлять токен заранее, если он скоро истечет
- Или обновлять токены в payload при каждой попытке

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### 4. **Нет очистки старых "sent" задач**

**Проблема:**
- Задачи со статусом "sent" остаются в БД навсегда
- Это может привести к:
  - Росту размера БД
  - Медленным запросам
  - Проблемам с производительностью

**Решение:**
- Добавить периодическую очистку старых "sent" задач (например, старше 7 дней)
- Или удалять "sent" задачи сразу после успешной синхронизации

---

### 5. **Нет приоритизации задач**

**Проблема:**
- Все задачи обрабатываются в порядке `created_at ASC`
- Нет различия между:
  - Критичными задачами (start/stop)
  - Некритичными задачами (screenshots)
  - Старыми задачами (могут быть устаревшими)

**Решение:**
- Добавить приоритеты задач
- Обрабатывать критичные задачи первыми
- Или использовать отдельные очереди для разных типов задач

---

### 6. **Нет ограничения на размер очереди**

**Проблема:**
- Очередь может расти бесконечно
- Это может привести к:
  - Переполнению БД
  - Проблемам с памятью
  - Медленной работе приложения

**Решение:**
- Добавить максимальный размер очереди (например, 1000 задач)
- При достижении лимита:
  - Удалять самые старые задачи
  - Или отклонять новые задачи с ошибкой

---

### 7. **Нет обработки устаревших задач**

**Проблема:**
- Задачи могут быть устаревшими (например, pause для уже остановленного time entry)
- Сервер может вернуть ошибку (например, 404 или 400)
- Задача будет повторяться 5 раз, затем станет failed

**Решение:**
- Проверять устаревшие задачи перед синхронизацией
  - Для pause/resume/stop: проверять, существует ли time entry
  - Для start: проверять, не создан ли уже time entry
- Помечать устаревшие задачи как "obsolete" вместо "failed"

---

### 8. **Exponential backoff может быть слишком агрессивным**

**Проблема:**
- Exponential backoff: 1 min, 2 min, 4 min, 8 min, 16 min, ...
- Для retry_count = 4 → ждать 16 минут
- Это может быть слишком долго для пользователя

**Решение:**
- Уменьшить интервалы (например, 30 sec, 1 min, 2 min, 4 min, 8 min)
- Или использовать более мягкий backoff (линейный или квадратичный)

---

## 🟢 НИЗКИЕ ПРОБЛЕМЫ

### 9. **Нет метрик и мониторинга**

**Проблема:**
- Нет статистики:
  - Сколько задач синхронизировано за день
  - Среднее время синхронизации
  - Процент успешных/неуспешных синхронизаций
- Сложно диагностировать проблемы

**Решение:**
- Добавить метрики (например, через `tracing` или отдельную структуру)
- Логировать статистику периодически

---

### 10. **Нет обработки частичных ошибок**

**Проблема:**
- Если часть задач синхронизирована успешно, а часть нет, нет информации о том, какие именно
- Пользователь не знает, что именно не синхронизировалось

**Решение:**
- Возвращать детальную информацию о результатах синхронизации
- Показывать пользователю, какие задачи не синхронизировались

---

## 📊 СТАТИСТИКА

### Текущее состояние:
- ✅ Защита от дублирования (5 секунд)
- ✅ Сохранение причины ошибки
- ✅ Exponential backoff
- ✅ Автоматическое обновление токенов при 401
- ✅ Фоновая синхронизация (каждую минуту)
- ❌ Ограничение на 10 задач за раз
- ❌ Нет защиты от параллельных вызовов
- ❌ Нет очистки старых задач
- ❌ Нет приоритизации

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЯ

### Высокий приоритет:
1. **Увеличить LIMIT до 50-100** (проблема #1)
2. **Добавить защиту от параллельных вызовов** (проблема #2)
3. **Проверять срок действия токенов** (проблема #3)

### Средний приоритет:
4. **Добавить очистку старых "sent" задач** (проблема #4)
5. **Добавить приоритизацию задач** (проблема #5)
6. **Обрабатывать устаревшие задачи** (проблема #7)

### Низкий приоритет:
7. **Добавить ограничение на размер очереди** (проблема #6)
8. **Уменьшить exponential backoff** (проблема #8)
9. **Добавить метрики** (проблема #9)
10. **Улучшить обработку частичных ошибок** (проблема #10)

---

**Дата создания:** 2025-01-08
