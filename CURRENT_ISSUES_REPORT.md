# Отчет о текущих проблемах системы Hubnity

**Дата:** 2025-01-08  
**Версия:** 0.1.0  
**Статус анализа:** Актуальный

---

## 📊 КРАТКОЕ РЕЗЮМЕ

### Статистика проблем:

- 🔴 **Критические:** 3
- 🟡 **Средние:** 8
- 🟢 **Низкие:** 5
- ✅ **Исправлено недавно:** 2

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Active Window Tracking отключен ❌

**Статус:** Временно отключено  
**Приоритет:** ВЫСОКИЙ  
**Файл:** `src-tauri/src/lib.rs:2117-2136`

**Проблема:**

- Функция `get_active_window_info()` отключена из-за Objective-C exceptions
- Rust не может перехватить foreign exceptions через `panic::catch_unwind`
- URL tracking не работает (возвращает пустые данные)

**Текущее состояние:**

```rust
// ВРЕМЕННО ОТКЛЮЧЕНО: Objective-C exceptions не могут быть перехвачены
warn!("[ACTIVE_WINDOW] Function temporarily disabled");
return Ok(ActiveWindowInfo {
    app_name: None,
    window_title: None,
    url: None,
    domain: None,
});
```

**Решение:**

- Реализовать Objective-C @try/@catch через FFI wrapper
- Или использовать AppleScript как альтернативу
- Или использовать безопасные обертки Accessibility API

**Время на исправление:** 2-3 дня

---

### 2. Шифрование ключа использует дефолтный ключ ⚠️

**Статус:** Работает, но небезопасно  
**Приоритет:** ВЫСОКИЙ  
**Файл:** `src-tauri/src/lib.rs:30-39`

**Проблема:**

- Используется дефолтный ключ шифрования: `"default-encryption-key-32-bytes!"`
- Ключ не хранится в Keychain (macOS)
- Токены шифруются, но ключ известен

**Текущее состояние:**

```rust
// TODO: В production использовать Keychain для хранения ключа
// Для сейчас используем дефолтный ключ (НЕ БЕЗОПАСНО для production!)
let key = std::env::var("HUBNITY_ENCRYPTION_KEY")
    .ok()
    .and_then(|k| hex::decode(k).ok())
    .unwrap_or_else(|| {
        b"default-encryption-key-32-bytes!".to_vec()
    });
```

**Решение:**

- Использовать macOS Keychain для хранения ключа
- Генерировать уникальный ключ при первом запуске
- Сохранять ключ в Keychain, а не в переменных окружения

**Время на исправление:** 1-2 дня

---

### 3. Ошибки сохранения состояния игнорируются ⚠️

**Статус:** Работает, но может терять данные  
**Приоритет:** ВЫСОКИЙ  
**Файл:** `src-tauri/src/lib.rs:1308-1313, 1399-1402`

**Проблема:**

- Ошибки сохранения состояния в БД логируются, но не возвращаются
- Состояние может быть не сохранено, но операция считается успешной
- Риск потери данных при сбоях БД

**Текущее состояние:**

```rust
if let Err(e) = self.save_state() {
    eprintln!("[TIMER] Failed to save state after pause: {}", e);
    // Ошибка не возвращается - состояние может быть не сохранено
}
Ok(())
```

**Решение:**

- Возвращать ошибки из критических операций
- Показывать пользователю предупреждение при ошибках сохранения
- Реализовать retry механизм для сохранения состояния

**Время на исправление:** 1 день

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### 4. Использование eprintln! вместо структурированного логирования

**Статус:** Частично исправлено  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src-tauri/src/lib.rs` (множественные места)

**Проблема:**

- В коде остались `eprintln!` для ошибок скриншотов
- Не используется структурированное логирование через `tracing`
- Логи могут быть менее информативными

**Места:**

- `[SCREENSHOT ERROR]` - 7 мест
- `[TIMER]` - несколько мест

**Решение:**

- Заменить все `eprintln!` на `error!` из `tracing`
- Использовать структурированные логи с контекстом

**Время на исправление:** 2-3 часа

---

### 5. Множественные вызовы checkAndStartScreenshots

**Статус:** Работает, но неоптимально  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src/App.tsx:611-653`

**Проблема:**

- `checkAndStartScreenshots()` вызывается при каждом изменении store
- Также вызывается каждые 2 секунды через `setInterval`
- Может приводить к дублированию логики

**Текущее состояние:**

```typescript
// Subscribe to store changes
unsubscribe = useTrackerStore.subscribe(() => {
  checkAndStartScreenshots().catch(() => {});
});

// Check periodically (every 2 seconds)
const checkInterval = setInterval(() => {
  checkAndStartScreenshots().catch(() => {});
}, 2000);
```

**Решение:**

- Оптимизировать логику вызовов
- Использовать debounce для подписки на изменения store
- Убрать дублирование проверок

**Время на исправление:** 2-3 часа

---

### 6. Отсутствие валидации payload в очереди

**Статус:** Работает, но может падать  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src-tauri/src/lib.rs:sync_task`

**Проблема:**

- Payload десериализуется без валидации
- Могут быть ошибки при невалидных данных
- Нет проверки обязательных полей

**Решение:**

- Добавить валидацию payload перед десериализацией
- Проверять наличие обязательных полей
- Возвращать понятные ошибки валидации

**Время на исправление:** 1 день

---

### 7. Ошибки "проглатываются" в catch блоках

**Статус:** Работает, но теряется информация  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src/store/useTrackerStore.ts` (множественные места)

**Проблема:**

- Множество `.catch(() => {})` без логирования
- Ошибки теряются, сложно отлаживать
- Нет мониторинга ошибок

**Примеры:**

```typescript
await invoke("log_message", { message: "..." }).catch(() => {});
timerState = await TimerEngineAPI.getState().catch(() => null);
```

**Решение:**

- Добавить логирование ошибок в catch блоках
- Использовать централизованный error handler
- Отслеживать критические ошибки

**Время на исправление:** 1 день

---

### 8. Нет обработки race conditions в startTracking

**Статус:** Работает, но может быть проблема  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src/store/useTrackerStore.ts:165-366`

**Проблема:**

- Проверка `isLoading` может не предотвратить все race conditions
- Между проверкой состояния таймера и вызовом `start()` может пройти время
- Возможны дублирующие вызовы API

**Решение:**

- Добавить более строгую проверку состояния
- Использовать atomic операции
- Добавить retry логику для конфликтов

**Время на исправление:** 1 день

---

### 9. Отсутствие лимитов на размер очереди

**Статус:** Работает, но может переполниться  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src-tauri/src/lib.rs:Database::enqueue_sync`

**Проблема:**

- Нет ограничения на количество задач в очереди
- При длительном offline может накопиться много задач
- Может привести к переполнению БД

**Решение:**

- Добавить лимит на количество задач в очереди
- Реализовать приоритизацию задач
- Удалять старые задачи при переполнении

**Время на исправление:** 1 день

---

### 10. Нет мониторинга фоновой синхронизации

**Статус:** Работает, но нет метрик  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src-tauri/src/lib.rs:2341-2367`

**Проблема:**

- Фоновая синхронизация работает, но нет метрик
- Неизвестно, сколько задач обработано
- Нет уведомлений об ошибках синхронизации

**Решение:**

- Добавить метрики синхронизации
- Логировать статистику обработки задач
- Показывать пользователю статус синхронизации

**Время на исправление:** 1 день

---

### 11. Отсутствие обработки истечения refresh token

**Статус:** Работает, но может сломаться  
**Приоритет:** СРЕДНИЙ  
**Файл:** `src-tauri/src/lib.rs:sync_task`

**Проблема:**

- Если refresh token истек, задачи не отправятся
- Нет обработки случая истечения refresh token
- Пользователь не будет уведомлен

**Решение:**

- Проверять срок действия refresh token
- Показывать пользователю предупреждение при истечении
- Предлагать перелогиниться

**Время на исправление:** 1 день

---

## 🟢 НИЗКИЕ ПРОБЛЕМЫ

### 12. Дублирование логики проверки состояния таймера

**Статус:** Работает, но можно улучшить  
**Приоритет:** НИЗКИЙ  
**Файл:** `src/store/useTrackerStore.ts`

**Проблема:**

- Логика проверки состояния таймера дублируется в нескольких местах
- Можно вынести в отдельную функцию

**Решение:**

- Создать helper функцию для проверки состояния
- Использовать её во всех местах

**Время на исправление:** 1-2 часа

---

### 13. Отсутствие unit тестов для SyncManager

**Статус:** Работает, но не протестировано  
**Приоритет:** НИЗКИЙ  
**Файл:** `src-tauri/src/lib.rs:SyncManager`

**Проблема:**

- Нет unit тестов для SyncManager
- Сложно проверить корректность работы очереди

**Решение:**

- Написать unit тесты для SyncManager
- Протестировать retry механизм
- Протестировать обработку ошибок

**Время на исправление:** 1 день

---

### 14. Нет документации по API очереди

**Статус:** Работает, но не документировано  
**Приоритет:** НИЗКИЙ  
**Файл:** `src-tauri/src/lib.rs`

**Проблема:**

- Нет документации по использованию очереди
- Неясно, как работает синхронизация
- Сложно понять формат payload

**Решение:**

- Добавить документацию по API очереди
- Описать формат payload для каждого типа задачи
- Добавить примеры использования

**Время на исправление:** 2-3 часа

---

### 15. Предупреждения компилятора о dead code

**Статус:** Работает, но есть предупреждения  
**Приоритет:** НИЗКИЙ  
**Файл:** `src-tauri/src/lib.rs`

**Проблема:**

- Функции `extract_url_from_title` и `extract_domain` помечены как `#[allow(dead_code)]`
- Используются только в закомментированном коде

**Решение:**

- Оставить как есть (код будет использован в будущем)
- Или удалить, если не планируется использовать

**Время на исправление:** 5 минут

---

### 16. Отсутствие обработки edge cases в URL tracking

**Статус:** Работает, но может быть проблема  
**Приоритет:** НИЗКИЙ  
**Файл:** `src/App.tsx:trackUrlActivity`

**Проблема:**

- Нет обработки случая, когда URL меняется очень быстро
- Может быть потеря данных при частых переключениях

**Решение:**

- Добавить debounce для частых изменений URL
- Сохранять минимальное время для каждого URL

**Время на исправление:** 2-3 часа

---

## ✅ НЕДАВНО ИСПРАВЛЕННЫЕ ПРОБЛЕМЫ

### 1. FSM Error: Invalid transition Running → Running ✅

**Статус:** Исправлено  
**Дата:** 2025-01-08

**Проблема:**

- Вызывался `start()` когда таймер уже запущен
- Появлялось предупреждение в логах

**Решение:**

- Добавлена проверка состояния таймера перед вызовом `start()`
- Если таймер уже запущен, ничего не делается
- Если на паузе, вызывается `resume()`

---

### 2. Предупреждения компилятора ✅

**Статус:** Исправлено  
**Дата:** 2025-01-08

**Проблема:**

- Неиспользуемый импорт `debug`
- Функции помечены как dead code

**Решение:**

- Удален неиспользуемый импорт
- Добавлены `#[allow(dead_code)]` для функций, используемых в закомментированном коде

---

## 📊 ПРИОРИТЕТЫ ИСПРАВЛЕНИЯ

### Высокий приоритет (критично для production):

1. ✅ Active Window Tracking (2-3 дня)
2. ✅ Шифрование ключа через Keychain (1-2 дня)
3. ✅ Обработка ошибок сохранения состояния (1 день)

### Средний приоритет (важно для стабильности):

4. Замена eprintln! на структурированное логирование (2-3 часа)
5. Оптимизация checkAndStartScreenshots (2-3 часа)
6. Валидация payload (1 день)
7. Логирование ошибок в catch блоках (1 день)
8. Обработка race conditions (1 день)
9. Лимиты на размер очереди (1 день)
10. Мониторинг синхронизации (1 день)
11. Обработка истечения refresh token (1 день)

### Низкий приоритет (можно отложить):

12-16. Различные улучшения и оптимизации

---

## 🎯 РЕКОМЕНДАЦИИ

### Краткосрочно (1 неделя):

1. Исправить критические проблемы (1-3)
2. Заменить eprintln! на структурированное логирование
3. Добавить валидацию payload

### Среднесрочно (2-3 недели):

4. Оптимизировать вызовы checkAndStartScreenshots
5. Добавить логирование ошибок
6. Реализовать лимиты очереди
7. Добавить мониторинг синхронизации

### Долгосрочно (1 месяц):

8. Реализовать Active Window Tracking через безопасный метод
9. Перейти на Keychain для хранения ключа
10. Написать unit тесты для SyncManager
11. Добавить документацию

---

**Дата отчета:** 2025-01-08  
**Следующее обновление:** После исправления критических проблем
