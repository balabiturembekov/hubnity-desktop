# BUSINESS LOGIC ISSUES - Time Tracker

**Дата:** 2025-01-08  
**Роль:** Senior QA Engineer  
**Фокус:** Несоответствия бизнес-логике time tracker

---

## 🟡 MEDIUM ISSUE #1: Неправильная проверка состояния в loadActiveTimeEntry

**Файл:** `src/store/useTrackerStore.ts`  
**Строки:** 131-135

**Проблема:**
Используется `timerState.state.state === 'RUNNING'` вместо `timerState.state === 'RUNNING'` (из-за `#[serde(flatten)]` в Rust).

**Код:**
```typescript
const timerState = await TimerEngineAPI.getState();
if (timerState.state.state === 'RUNNING') {  // ❌ Неправильно
  await TimerEngineAPI.pause();
}
```

**Опасность:**
- Проверка никогда не сработает (TypeScript ошибка компиляции или runtime ошибка)
- Таймер не будет синхронизирован с сервером при загрузке PAUSED entry

**Приоритет:** 🟡 **MEDIUM**

---

## 🟡 MEDIUM ISSUE #2: Смена проекта во время трекинга не останавливает таймер

**Файл:** `src/store/useTrackerStore.ts`  
**Строки:** 165-176

**Проблема:**
Пользователь может сменить проект во время активного трекинга. Таймер продолжает работать для старого проекта, но `selectedProject` меняется.

**Текущее поведение:**
```typescript
selectProject: (project: Project) => {
  const { isTracking, currentTimeEntry } = get();
  
  // Warn if changing project while tracking (but allow it - user might want to switch)
  if (isTracking && currentTimeEntry) {
    console.warn(`Changing project while tracking...`);
    // Note: We don't prevent this, but it might cause confusion
  }
  
  set({ selectedProject: project });
}
```

**Бизнес-логика:**
В большинстве time tracker приложений (Hubstaff, TimeDoctor):
- При смене проекта во время трекинга таймер должен **остановиться**
- Или показать предупреждение и требовать подтверждения
- Текущее поведение может привести к путанице: таймер работает для проекта A, но выбран проект B

**Приоритет:** 🟡 **MEDIUM** (зависит от требований продукта)

---

## 🟡 MEDIUM ISSUE #3: Нет защиты от rapid clicks (spam) на кнопки

**Файл:** `src/components/Timer.tsx`  
**Строки:** 146-178

**Проблема:**
Пользователь может быстро кликнуть кнопку "Start" 10 раз подряд. Хотя FSM в Rust защищает от недопустимых переходов, это приводит к:
- Множественным вызовам API
- Потенциальным race conditions
- Неоптимальному использованию ресурсов

**Текущая защита:**
- `disabled={isLoading}` - защищает только во время загрузки
- Но между вызовами API может быть окно для spam

**Решение:**
Добавить debounce/throttle для кнопок или локальный флаг `isProcessing`.

**Приоритет:** 🟡 **MEDIUM**

---

## 🟢 LOW ISSUE #4: Восстановление RUNNING как PAUSED - намеренное поведение

**Файл:** `src-tauri/src/lib.rs`  
**Строки:** 1932-1936

**Проблема:**
При восстановлении состояния из БД, если было `"running"`, оно восстанавливается как `Paused`:

```rust
"running" => {
    // Если было running, восстанавливаем как paused (безопаснее)
    // Пользователь может возобновить вручную
    TimerState::Paused
}
```

**Анализ:**
- Это **намеренное поведение** для безопасности
- Если приложение крашнулось во время RUNNING, время текущей сессии теряется
- Но `accumulated_seconds` сохраняется
- Это правильное поведение, но может быть неожиданным для пользователя

**Рекомендация:**
Добавить уведомление пользователю: "Таймер был приостановлен после перезапуска приложения. Нажмите 'Возобновить' для продолжения."

**Приоритет:** 🟢 **LOW** (это правильное поведение, но можно улучшить UX)

---

## 🟢 LOW ISSUE #5: Sleep detection threshold 5 минут может быть слишком агрессивным

**Файл:** `src-tauri/src/lib.rs`  
**Строки:** 2383

**Проблема:**
Sleep detection срабатывает при пропуске > 5 минут. Это может быть слишком агрессивно для реальной работы:
- Пользователь может уйти на обед (30-60 минут)
- Пользователь может быть на встрече (15-30 минут)
- Пользователь может работать над задачей без активности (10-15 минут)

**Текущее поведение:**
- При пропуске > 5 минут таймер автоматически паузится
- Это может прервать реальную работу

**Рекомендация:**
- Увеличить threshold до 10-15 минут
- Или сделать настраиваемым в настройках
- Или добавить предупреждение вместо автоматической паузы

**Приоритет:** 🟢 **LOW** (зависит от требований продукта)

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. ✅ FSM строгая и защищает от недопустимых переходов
2. ✅ Sleep/Wake обрабатывается корректно
3. ✅ Day rollover работает правильно
4. ✅ Clock skew detection реализован
5. ✅ SQLite транзакции используются
6. ✅ Все методы FSM теперь сохраняют состояние в БД
7. ✅ Восстановление состояния из БД безопасное

---

## 📋 ПЛАН ИСПРАВЛЕНИЯ

### Приоритет 1 (Важно):
1. **ISSUE #1**: Исправить проверку состояния в `loadActiveTimeEntry`

### Приоритет 2 (Зависит от требований):
2. **ISSUE #2**: Решить, нужно ли останавливать таймер при смене проекта
3. **ISSUE #3**: Добавить защиту от rapid clicks

### Приоритет 3 (Улучшение UX):
4. **ISSUE #4**: Добавить уведомление при восстановлении RUNNING как PAUSED
5. **ISSUE #5**: Рассмотреть увеличение sleep detection threshold

---

**Отчет подготовлен:** 2025-01-08  
**Найдено 1 средняя проблема, 4 низких (включая улучшения UX)**
