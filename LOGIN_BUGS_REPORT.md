# ОТЧЕТ О БАГАХ В ФОРМЕ АВТОРИЗАЦИИ

## 🔴 КРИТИЧЕСКИЙ БАГ #1: Токены сохраняются до проверки ответа

**Файл:** `src/lib/api.ts`  
**Строки:** 277-281

**Проблема:**
Токены сохраняются в `api.login()` ДО того, как `useAuthStore.login()` проверит ответ и обновит состояние. Если что-то пойдет не так в `useAuthStore.login()` (например, ошибка при сохранении в Zustand persist), токены останутся в localStorage без авторизованного пользователя.

**Код:**
```typescript
async login(credentials: LoginRequest): Promise<LoginResponse> {
  const response = await this.client.post<LoginResponse>('/auth/login', credentials);
  this.setToken(response.data.access_token);  // ⚠️ Сохраняется сразу
  localStorage.setItem('refresh_token', response.data.refresh_token);  // ⚠️ Сохраняется сразу
  return response.data;
}
```

**Опасность:**
- Токены могут остаться в localStorage без авторизованного пользователя
- При следующем запуске приложения может возникнуть рассинхронизация
- Пользователь может быть "авторизован" по токенам, но не иметь данных пользователя в store

**Решение:**
Сохранять токены только после успешного обновления состояния в `useAuthStore`. Или переместить сохранение токенов в `useAuthStore.login()` после успешного обновления состояния.

---

## 🟡 СРЕДНИЙ БАГ #2: Ошибка не очищается при изменении полей

**Файл:** `src/components/Login.tsx`  
**Строки:** 82, 94

**Проблема:**
Ошибка не очищается автоматически при изменении email или password. Пользователь может исправить данные, но ошибка останется видимой до следующей попытки входа.

**Код:**
```typescript
<Input
  onChange={(e) => setEmail(e.target.value)}  // ⚠️ Не очищает ошибку
  ...
/>
<Input
  onChange={(e) => setPassword(e.target.value)}  // ⚠️ Не очищает ошибку
  ...
/>
```

**Опасность:**
- Плохой UX - пользователь видит старую ошибку даже после исправления данных
- Может ввести в заблуждение пользователя

**Решение:**
Добавить очистку ошибки в обработчики `onChange`:
```typescript
onChange={(e) => {
  setEmail(e.target.value);
  if (error) setError(null);  // Очищаем ошибку
}}
```

---

## 🟢 НИЗКИЙ БАГ #3: Нет проверки на пустой email перед валидацией формата

**Файл:** `src/components/Login.tsx`  
**Строки:** 23-28

**Проблема:**
Если email пустой, сначала показывается ошибка валидации формата "Введите корректный email адрес", а не более понятное сообщение "Введите email".

**Код:**
```typescript
// Basic email validation
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (!emailRegex.test(email)) {  // ⚠️ Не проверяет пустоту отдельно
  setError('Введите корректный email адрес');
  return;
}
```

**Опасность:**
- Небольшое ухудшение UX
- Сообщение об ошибке менее понятное для пустого поля

**Решение:**
Добавить отдельную проверку на пустоту:
```typescript
if (!email || !email.trim()) {
  setError('Введите email');
  return;
}
if (!emailRegex.test(email.trim())) {
  setError('Введите корректный email адрес');
  return;
}
```

---

## 🟢 НИЗКИЙ БАГ #4: Пароль не обрезается (trim)

**Файл:** `src/components/Login.tsx`  
**Строка:** 40

**Проблема:**
Email обрезается (`email.trim()`), но пароль не обрезается. Это может привести к проблемам, если пользователь случайно добавит пробелы в начале или конце пароля.

**Код:**
```typescript
await login({ email: email.trim(), password });  // ⚠️ password не обрезается
```

**Опасность:**
- Пользователь может случайно добавить пробелы в пароль
- Может привести к ошибке входа, даже если пароль правильный

**Решение:**
Обрезать пароль (но только в UI, не изменять сам пароль в состоянии):
```typescript
await login({ email: email.trim(), password: password.trim() });
```

**Примечание:** Некоторые системы могут требовать точного совпадения пароля с пробелами, поэтому это нужно проверить с бэкендом.

---

## 🟢 НИЗКИЙ БАГ #5: Email не очищается после успешного входа

**Файл:** `src/components/Login.tsx`  
**Строки:** 40-42

**Проблема:**
После успешного входа очищается только пароль, но не email. Это может быть проблемой безопасности, если пользователь использует общий компьютер.

**Код:**
```typescript
await login({ email: email.trim(), password });
// Clear password after successful login (security best practice)
setPassword('');  // ⚠️ Email не очищается
```

**Опасность:**
- Небольшая проблема безопасности на общих компьютерах
- Email остается видимым в форме

**Решение:**
Очистить оба поля после успешного входа:
```typescript
await login({ email: email.trim(), password });
// Clear credentials after successful login (security best practice)
setPassword('');
setEmail('');
```

---

## 📊 ИТОГО

- **Критических багов:** 1
- **Средних багов:** 1
- **Низких багов:** 3

**Рекомендации:**
1. **СРОЧНО исправить БАГ #1** - это может привести к рассинхронизации состояния
2. Исправить БАГ #2 для улучшения UX
3. Исправить БАГ #3, #4, #5 для полировки формы
