# 🔍 Sentry Integration Setup

## Обзор

Sentry интегрирован в приложение для мониторинга ошибок в production. Интеграция включает:

- ✅ Инициализация в `main.tsx`
- ✅ Интеграция в `ErrorBoundary` для React ошибок
- ✅ Интеграция в `logger` utility для всех ошибок
- ✅ Установка пользовательского контекста при логине
- ✅ Фильтрация чувствительных данных (токены, пароли)
- ✅ Настройка окружений (dev/prod)

## Настройка

### 1. Создайте проект в Sentry

1. Зайдите на https://sentry.io
2. Создайте новый проект типа "React"
3. Скопируйте DSN из настроек проекта

### 2. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
VITE_SENTRY_DSN=your-sentry-dsn-here
VITE_APP_VERSION=0.1.0
```

**Важно:** Добавьте `.env` в `.gitignore`, чтобы не коммитить DSN в репозиторий.

### 3. Проверка работы

Sentry автоматически:

- Инициализируется при старте приложения (только в production)
- Отправляет ошибки из ErrorBoundary
- Отправляет ошибки из logger.error() и logger.logError()
- Устанавливает пользовательский контекст при логине
- Фильтрует токены и пароли перед отправкой

## Фильтрация данных

Sentry автоматически фильтрует:

- Токены из URL (`access_token`, `refresh_token`, `token`)
- Authorization headers
- Поля с `token` или `password` в названии
- Данные из breadcrumbs

## Окружения

- **Development:** Sentry не инициализируется (или использует dev DSN)
- **Production:** Sentry инициализируется с production DSN из переменной окружения

## Отключение Sentry

Если нужно временно отключить Sentry:

1. Не устанавливайте `VITE_SENTRY_DSN` в `.env`
2. Или закомментируйте `initSentry()` в `main.tsx`

## Source Maps

Для production builds рекомендуется настроить source maps:

1. В `vite.config.ts` уже настроен build с source maps
2. Загрузите source maps в Sentry через CLI или автоматически через плагин

## Мониторинг

После настройки вы сможете видеть:

- Все ошибки React компонентов
- Все ошибки из catch блоков
- Stack traces с контекстом
- Информацию о пользователе (ID, email)
- Breadcrumbs для отладки

## Дополнительная информация

- [Sentry React Documentation](https://docs.sentry.io/platforms/javascript/guides/react/)
- [Sentry Session Replay](https://docs.sentry.io/platforms/javascript/session-replay/)
