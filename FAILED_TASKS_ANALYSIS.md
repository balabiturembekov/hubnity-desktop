# АНАЛИЗ FAILED ЗАДАЧ

**Дата:** 2025-01-08  
**Проблема:** Накопилось 100 failed задач  
**Статус:** Диагностика завершена, инструменты добавлены

---

## 📊 АНАЛИЗ ДАННЫХ

### Паттерны в failed задачах:

1. **Дублирование задач:**
   - Много задач создано в одно и то же время
   - Пример: 08.01.2026, 16:37:39-40 - 5-6 задач "Пауза" одновременно
   - Пример: 08.01.2026, 17:08-17:18 - множество задач "Пауза" с интервалом в 30 секунд

2. **Все задачи failed после 5 попыток:**
   - Все задачи имеют `retry_count = 5`
   - Это означает, что проблема была **постоянной**, не временная сеть

3. **Разные типы задач failed:**
   - Старт трекера (time_entry_start)
   - Пауза (time_entry_pause) - **наиболее часто**
   - Возобновление (time_entry_resume)
   - Остановка (time_entry_stop)

4. **Временной паттерн:**
   - Задачи создавались в течение дня (08.01.2026)
   - Последние попытки были в разное время
   - Это указывает на то, что синхронизация пыталась работать, но все попытки провалились

---

## 🔍 ВОЗМОЖНЫЕ ПРИЧИНЫ

### 1. **Дублирование задач (КРИТИЧНО)**

**Проблема:**
- Много задач создано одновременно (5-6 задач "Пауза" в одну секунду)
- Это указывает на проблему с защитой от дублирования

**Возможные причины:**
- Быстрые повторные клики пользователя
- Несколько вызовов `pauseTracking()` подряд
- Проблема с защитой `isProcessing` в UI
- Race condition между frontend и backend

**Решение:**
- ✅ Уже есть защита `isProcessing` в `Timer.tsx`
- ⚠️ Нужно проверить, работает ли она правильно
- ⚠️ Возможно, нужна дополнительная защита на уровне Rust

---

### 2. **Все задачи failed после 5 попыток**

**Проблема:**
- Все задачи имеют `retry_count = 5` (максимум)
- Это означает, что проблема была **постоянной**

**Возможные причины:**
- **401 ошибки (токены истекли):** Все запросы получали 401, refresh token не работал
- **4xx ошибки (неправильный запрос):** Формат запроса изменился, сервер не принимает
- **5xx ошибки (проблемы сервера):** Сервер был недоступен или возвращал ошибки
- **Неправильный API endpoint:** URL изменился или неправильный

**Решение:**
- ✅ Добавлено сохранение причины ошибки в БД
- ✅ Добавлено отображение причины ошибки в диалоге
- ✅ Добавлено отображение payload для диагностики

---

### 3. **Проблема с токенами (ВЕРОЯТНО)**

**Признаки:**
- Все задачи failed
- Разные типы задач (start, pause, resume, stop)
- Все после 5 попыток

**Если это проблема с токенами:**
- Токены истекли
- Refresh token не работает
- Все запросы получают 401
- После 5 попыток задачи помечаются как failed

**Решение:**
- Проверить логи на наличие "Token expired (401)"
- Проверить, работает ли refresh token
- Попробовать выйти и войти заново
- Повторить синхронизацию после обновления токенов

---

## ✅ РЕАЛИЗОВАННЫЕ УЛУЧШЕНИЯ

### 1. Сохранение причины ошибки

**Backend:**
- ✅ Добавлена колонка `error_message` в таблицу `sync_queue`
- ✅ Миграция для существующих БД
- ✅ Сохранение причины ошибки при пометке задачи как failed

**Типы ошибок, которые сохраняются:**
- `HTTP 401: Unauthorized` - токены истекли
- `HTTP 404: Not Found` - неправильный endpoint
- `HTTP 500: Internal Server Error` - проблема сервера
- `Network error: ...` - ошибка сети
- `Token refresh failed: ...` - ошибка обновления токена

### 2. Улучшенный диалог failed задач

**Frontend:**
- ✅ Отображение причины ошибки для каждой задачи
- ✅ Отображение payload (без токенов) для диагностики
- ✅ Раскрывающийся блок "Показать payload"
- ✅ Более информативное отображение

### 3. Кнопка "Повторить все"

**Функционал:**
- ✅ Сброс failed задач обратно в pending
- ✅ Автоматический запуск синхронизации
- ✅ Обновление индикатора синхронизации

---

## 🔧 КАК ДИАГНОСТИРОВАТЬ

### Шаг 1: Открыть диалог с деталями

1. Кликните на индикатор "Ошибка (100)"
2. Откроется диалог со списком failed задач

### Шаг 2: Проанализировать ошибки

**Проверьте:**
1. **Причину ошибки** - что написано в "Причина ошибки"?
   - Если `HTTP 401` → проблема с токенами
   - Если `HTTP 404` → неправильный endpoint
   - Если `HTTP 500` → проблема сервера
   - Если `Network error` → проблема сети

2. **Payload задачи** - нажмите "Показать payload"
   - Проверьте, что данные правильные
   - Проверьте, что нет лишних полей

3. **Время создания** - когда задачи были созданы?
   - Если все в одно время → проблема с дублированием
   - Если в разное время → проблема с синхронизацией

### Шаг 3: Определить корневую причину

**Если все ошибки `HTTP 401`:**
- Проблема с токенами
- Решение: Выйти и войти заново, затем повторить синхронизацию

**Если все ошибки `HTTP 404`:**
- Неправильный API endpoint
- Решение: Проверить конфигурацию API URL

**Если все ошибки `HTTP 500`:**
- Проблема сервера
- Решение: Проверить логи сервера, связаться с администратором

**Если все ошибки `Network error`:**
- Проблема сети
- Решение: Проверить подключение к интернету

---

## 🎯 РЕКОМЕНДАЦИИ

### Немедленные действия:

1. **Откройте диалог failed задач** и посмотрите причину ошибки
2. **Проверьте payload** нескольких задач - все ли данные правильные?
3. **Проверьте логи** в терминале на наличие паттернов ошибок

### Если проблема с токенами:

1. Выйдите из приложения
2. Войдите заново
3. Нажмите "Повторить все" в диалоге failed задач
4. Проверьте, синхронизируются ли задачи

### Если проблема с дублированием:

1. Проверьте, работает ли защита `isProcessing` в UI
2. Проверьте логи на наличие множественных вызовов `pauseTracking()`
3. Возможно, нужна дополнительная защита на уровне Rust

### Если проблема с API:

1. Проверьте конфигурацию API URL
2. Проверьте, что endpoint правильный
3. Проверьте формат запроса (payload)

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. ✅ **Диагностика завершена** - инструменты добавлены
2. ⏳ **Пользователь должен проверить** - открыть диалог и посмотреть причину ошибки
3. ⏳ **Исправить проблему** - в зависимости от причины (токены, API, сеть)
4. ⏳ **Повторить синхронизацию** - нажать "Повторить все"

---

**Дата создания:** 2025-01-08
