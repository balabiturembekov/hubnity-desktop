# QA AUDIT REPORT - Time Tracker Business Logic

**Дата:** 2025-01-08  
**Роль:** Senior QA Engineer  
**Фокус:** Бизнес-логика и классические баги time tracker

---

## 🔴 CRITICAL BUG #1: resume() не сохраняет состояние в БД

**Файл:** `src-tauri/src/lib.rs`  
**Строки:** 2210-2248

**Проблема:**
Метод `resume()` переводит таймер из `Paused` в `Running`, но **НЕ сохраняет состояние в БД** после перехода. Это может привести к потере состояния при краше приложения.

**Код:**
```rust
fn resume(&self) -> Result<(), String> {
    // ...
    match &*state {
        TimerState::Paused => {
            // Переход в Running (accumulated сохраняется)
            *state = TimerState::Running {
                started_at: now_timestamp,
                started_at_instant: now_instant,
            };
            Ok(())  // ❌ НЕТ вызова save_state()!
        }
    }
}
```

**Сравнение с другими методами:**
- `start()` → сохраняет состояние ✅
- `pause()` → сохраняет состояние ✅
- `stop()` → сохраняет состояние ✅
- `resume()` → **НЕ сохраняет состояние** ❌

**Опасность:**
- Если приложение крашнется сразу после `resume()`, состояние будет потеряно
- При восстановлении из БД таймер будет в `Paused`, хотя должен быть в `Running`
- Накопленное время может быть потеряно

**Воспроизведение:**
1. Запустить таймер
2. Поставить на паузу
3. Возобновить (resume)
4. Принудительно закрыть приложение (kill -9)
5. Перезапустить приложение
6. **Ожидаемое:** Таймер в состоянии Running
7. **Фактическое:** Таймер в состоянии Paused (из БД)

**Приоритет:** 🔴 **CRITICAL**

---

## 🔴 CRITICAL BUG #2: start() при переходе Paused → Running не сохраняет состояние

**Файл:** `src-tauri/src/lib.rs`  
**Строки:** 2136-2150

**Проблема:**
В методе `start()` есть переход `Paused → Running` (через start вместо resume), но состояние **НЕ сохраняется в БД** после перехода.

**Код:**
```rust
TimerState::Paused => {
    // Допустимый переход: Paused → Running (resume через start)
    // ...
    *state = TimerState::Running {
        started_at: now_timestamp,
        started_at_instant: now_instant,
    };
    Ok(())  // ❌ НЕТ вызова save_state()!
}
```

**Опасность:**
- Аналогично BUG #1 - потеря состояния при краше
- Несоответствие с другими переходами в `start()`

**Приоритет:** 🔴 **CRITICAL**

---

## 🟡 MEDIUM BUG #3: Восстановление RUNNING как PAUSED может привести к потере времени

**Файл:** `src-tauri/src/lib.rs`  
**Строки:** 1932-1936

**Проблема:**
При восстановлении состояния из БД, если было `"running"`, оно восстанавливается как `Paused`:

```rust
"running" => {
    // Если было running, восстанавливаем как paused (безопаснее)
    // Пользователь может возобновить вручную
    TimerState::Paused
}
```

**Опасность:**
- Если приложение крашнулось во время RUNNING, время текущей сессии может быть потеряно
- `accumulated_seconds` сохраняется, но время текущей сессии (до краша) теряется
- Это правильное поведение для безопасности, но может быть неожиданным для пользователя

**Приоритет:** 🟡 **MEDIUM** (это намеренное поведение для безопасности)

---

## 🟡 MEDIUM BUG #4: Нет защиты от rapid clicks (spam) на frontend

**Файл:** `src/components/Timer.tsx`  
**Строки:** 146-177

**Проблема:**
Хотя FSM в Rust защищает от недопустимых переходов, на frontend нет защиты от rapid clicks. Пользователь может быстро кликнуть "Start" 10 раз подряд, что приведет к 10 вызовам API.

**Текущая защита:**
- `disabled={isLoading}` - защищает только во время загрузки
- Но между вызовами API может быть окно для spam

**Опасность:**
- Множественные вызовы API
- Потенциальные race conditions
- Неоптимальное использование ресурсов

**Приоритет:** 🟡 **MEDIUM**

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. ✅ FSM строгая и защищает от недопустимых переходов
2. ✅ Sleep/Wake обрабатывается корректно
3. ✅ Day rollover работает правильно
4. ✅ Clock skew detection реализован
5. ✅ SQLite транзакции используются
6. ✅ Recursion protection в get_state()
7. ✅ Восстановление состояния из БД безопасное

---

## 📋 ПЛАН ИСПРАВЛЕНИЯ

### Приоритет 1 (Критично):
1. **BUG #1**: Добавить `save_state()` в `resume()`
2. **BUG #2**: Добавить `save_state()` в `start()` для перехода Paused → Running

### Приоритет 2 (Важно):
3. **BUG #4**: Добавить debounce/throttle для кнопок на frontend

---

**Отчет подготовлен:** 2025-01-08  
**Найдено 2 критических бага, 2 средних**
